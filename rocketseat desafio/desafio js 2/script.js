

/*function calcularMedia() {

    let nome = document.getElementById('nome').value;

    let nota1 = prompt('Insira a nota da prova 1: ');
    nota1 = Number(nota1);

    let nota2 = prompt('Insira a nota da prova 2: ');
    nota2 = Number(nota2);

    let media = (nota1 + nota2) / 2;

    if (media < 7) {
        prompt(`Olá ${nome}, sua média foi de ${media}. Você foi reprovado.`);
    } else if (media === 7) {
        prompt(`Olá ${nome}, sua média foi de ${media}. Você passou.`);
    } else {
        prompt(`Olá ${nome}, sua média foi de ${media}. Parabéns, você está APROVADO!`);
    }
}

// Chamando a função
calcularMedia();*/
